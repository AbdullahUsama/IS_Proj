# 🏗️ SYSTEM ARCHITECTURE

## Visual System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER INTERFACE LAYER                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   main.py   │  │ run_steganalysis │  │ compare_methods  │  │
│  │             │  │      .py         │  │      .py         │  │
│  │ Interactive │  │  Steganalysis    │  │   Comparison     │  │
│  │   Program   │  │     Runner       │  │      Tool        │  │
│  └──────┬──────┘  └────────┬─────────┘  └────────┬─────────┘  │
│         │                  │                      │             │
└─────────┼──────────────────┼──────────────────────┼─────────────┘
          │                  │                      │
          ▼                  ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CORE FUNCTIONALITY LAYER                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐       ┌────────────────────────┐     │
│  │  adaptive_stego.py   │       │     AESCTR.py          │     │
│  │                      │       │                        │     │
│  │  ┌────────────────┐  │       │  ┌──────────────────┐  │     │
│  │  │ Edge Detection │  │       │  │  AES-256-CTR     │  │     │
│  │  │   (Sobel)      │  │       │  │  Encryption      │  │     │
│  │  └────────────────┘  │       │  └──────────────────┘  │     │
│  │  ┌────────────────┐  │       │  ┌──────────────────┐  │     │
│  │  │ 8×8 Blocks     │  │       │  │  Key Generation  │  │     │
│  │  │ Decomposition  │  │       │  └──────────────────┘  │     │
│  │  └────────────────┘  │       │  ┌──────────────────┐  │     │
│  │  ┌────────────────┐  │       │  │   Decryption     │  │     │
│  │  │ Mean-of-Medians│  │       │  └──────────────────┘  │     │
│  │  │  Computation   │  │       └────────────────────────┘     │
│  │  └────────────────┘  │                                       │
│  │  ┌────────────────┐  │                                       │
│  │  │ MSB Cases 0-3  │  │                                       │
│  │  │   Detection    │  │                                       │
│  │  └────────────────┘  │                                       │
│  │  ┌────────────────┐  │                                       │
│  │  │  Adaptive Bit  │  │                                       │
│  │  │   Embedding    │  │                                       │
│  │  └────────────────┘  │                                       │
│  └──────────────────────┘                                       │
│                                                                 │
│  ┌──────────────────────┐       ┌────────────────────────┐     │
│  │   metricscalc.py     │       │   steganalysis.py      │     │
│  │                      │       │                        │     │
│  │  ┌────────────────┐  │       │  ┌──────────────────┐  │     │
│  │  │ PSNR / MSE     │  │       │  │  RS Analysis     │  │     │
│  │  └────────────────┘  │       │  └──────────────────┘  │     │
│  │  ┌────────────────┐  │       │  ┌──────────────────┐  │     │
│  │  │ Entropy Calc   │  │       │  │  Histogram       │  │     │
│  │  └────────────────┘  │       │  │  Analysis        │  │     │
│  │  ┌────────────────┐  │       │  └──────────────────┘  │     │
│  │  │ Capacity       │  │       │  ┌──────────────────┐  │     │
│  │  └────────────────┘  │       │  │  Chi-Square      │  │     │
│  │  ┌────────────────┐  │       │  │  Attack          │  │     │
│  │  │ Histogram Dev  │  │       │  └──────────────────┘  │     │
│  │  └────────────────┘  │       └────────────────────────┘     │
│  └──────────────────────┘                                       │
│                                                                 │
│  ┌──────────────────────┐                                       │
│  │     steno.py         │                                       │
│  │  (Basic LSB - for    │                                       │
│  │   comparison only)   │                                       │
│  └──────────────────────┘                                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
          │                  │                      │
          ▼                  ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPPORTING LIBRARIES                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  OpenCV (cv2)  │  NumPy  │  Matplotlib  │  Cryptography        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

### Embedding Process

```
┌─────────────┐
│   Secret    │
│   Message   │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│      AES-CTR Encryption         │
│  (AESCTR.py)                    │
│  • Generate random key          │
│  • Generate random nonce        │
│  • Encrypt message              │
└────────────┬────────────────────┘
             │
             ▼
      ┌──────────────┐
      │  Encrypted   │
      │  Payload     │
      │ (nonce +     │
      │ ciphertext)  │
      └──────┬───────┘
             │
             ▼
┌────────────────────────────────────┐
│   Edge-Adaptive Steganography      │
│   (adaptive_stego.py)              │
│                                    │
│  1. Load cover image               │
│  2. Compute edge map (Sobel)       │
│  3. Partition into 8×8 blocks      │
│  4. Sort blocks by edge intensity  │
│  5. Embed UB/LB in first block     │
│  6. For each block:                │
│     • Compute mean-of-medians      │
│     • Check pixel difference ≤ Me  │
│     • Determine MSB case (0-3)     │
│     • Embed bits adaptively        │
└────────────┬───────────────────────┘
             │
             ▼
      ┌──────────────┐
      │    Stego     │
      │    Image     │
      └──────────────┘
```

### Extraction Process

```
┌─────────────┐
│    Stego    │
│    Image    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│  Edge-Adaptive Extraction       │
│  (adaptive_stego.py)            │
│                                 │
│  1. Load stego image            │
│  2. Compute edge map            │
│  3. Extract UB/LB from block 0  │
│  4. Extract payload length      │
│  5. Sort blocks (same order)    │
│  6. For each block:             │
│     • Compute mean-of-medians   │
│     • Check pixel difference    │
│     • Determine MSB case        │
│     • Extract bits              │
└────────────┬────────────────────┘
             │
             ▼
      ┌──────────────┐
      │  Encrypted   │
      │  Payload     │
      └──────┬───────┘
             │
             ▼
┌─────────────────────────────────┐
│    AES-CTR Decryption           │
│    (AESCTR.py)                  │
│  • Split nonce and ciphertext   │
│  • Decrypt with saved key       │
└────────────┬────────────────────┘
             │
             ▼
      ┌──────────────┐
      │   Original   │
      │   Message    │
      └──────────────┘
```

---

## 🔄 Module Interaction Diagram

```
                    ┌────────────────┐
                    │    main.py     │
                    │   (Control)    │
                    └───────┬────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌──────────────┐
│   AESCTR.py   │   │ adaptive_     │   │ metricscalc  │
│               │   │  stego.py     │   │    .py       │
│ • encrypt()   │   │               │   │              │
│ • decrypt()   │   │ • encode()    │   │ • PSNR()     │
└───────────────┘   │ • decode()    │   │ • MSE()      │
                    │               │   │ • Entropy()  │
                    └───────────────┘   └──────────────┘
                            │
                    ┌───────┴────────┐
                    │                │
                    ▼                ▼
            ┌──────────────┐  ┌────────────┐
            │   OpenCV     │  │   NumPy    │
            │   (cv2)      │  │            │
            │              │  │            │
            │ • imread()   │  │ • arrays   │
            │ • imwrite()  │  │ • math ops │
            │ • Sobel()    │  │            │
            └──────────────┘  └────────────┘
```

```
                  ┌──────────────────────┐
                  │  run_steganalysis.py │
                  │      (Control)       │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │   steganalysis.py    │
                  │                      │
                  │  ┌────────────────┐  │
                  │  │  RSAnalysis    │  │
                  │  │   • analyze()  │  │
                  │  └────────────────┘  │
                  │  ┌────────────────┐  │
                  │  │ Histogram      │  │
                  │  │ Analysis       │  │
                  │  │   • analyze()  │  │
                  │  │   • visualize()│  │
                  │  └────────────────┘  │
                  │  ┌────────────────┐  │
                  │  │ ChiSquare      │  │
                  │  │ Attack         │  │
                  │  │   • analyze()  │  │
                  │  └────────────────┘  │
                  └──────────────────────┘
```

---

## 🎯 Algorithm Flow: Adaptive Embedding

```
START: Cover Image + Encrypted Payload
│
├─► Compute Edge Map (Sobel)
│   │
│   ├─► Gradient X (sobelx)
│   ├─► Gradient Y (sobely)
│   └─► Edge Magnitude = √(sobelx² + sobely²)
│
├─► Partition into 8×8 Blocks
│   │
│   └─► Blocks[] = all 8×8 regions
│
├─► Score Each Block
│   │
│   └─► edge_score = mean(edge_map_in_block)
│
├─► Sort Blocks by Edge Score (descending)
│   │
│   └─► High edge score = priority for embedding
│
├─► Embed UB/LB in First 16 Pixels
│   │
│   ├─► UB = max(cover_image)
│   └─► LB = min(cover_image)
│
├─► Embed Payload Length (32 bits)
│
├─► FOR each block (in edge-sorted order):
│   │
│   ├─► IF edge_score < threshold: SKIP
│   │
│   ├─► Compute Mean-of-Medians (Me)
│   │   │
│   │   ├─► FOR each column:
│   │   │   └─► column_median = median(column)
│   │   │
│   │   └─► Me = mean(all_column_medians)
│   │
│   └─► FOR each pixel pair (p1, p2):
│       │
│       ├─► Compute Di = |p1 - p2|
│       │
│       ├─► IF Di > Me: SKIP (too different)
│       │
│       ├─► Determine MSB Case:
│       │   │
│       │   ├─► Case 0: MSB(p1)=0, MSB(p2)=0 → embed 2 bits
│       │   ├─► Case 1: MSB(p1)=1, MSB(p2)=0 → embed 3 bits
│       │   ├─► Case 2: MSB(p1)=0, MSB(p2)=1 → embed 3 bits
│       │   └─► Case 3: MSB(p1)=1, MSB(p2)=1 → embed 4 bits
│       │
│       └─► Embed bits in appropriate positions
│
└─► SAVE Stego Image
    │
    END
```

---

## 🔐 Security Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                      SECURITY LAYERS                          │
└───────────────────────────────────────────────────────────────┘

Layer 1: Cryptographic Protection
┌─────────────────────────────────────────────┐
│  AES-256-CTR Encryption                     │
│  • 256-bit key (2²⁵⁶ keyspace)             │
│  • 8-byte nonce                             │
│  • Stream cipher properties                 │
│  • Encrypted payload appears random         │
└─────────────────────────────────────────────┘
                    ▼
Layer 2: Steganographic Concealment
┌─────────────────────────────────────────────┐
│  Adaptive LSB-MSB Embedding                 │
│  • Multi-bit adaptive insertion             │
│  • MSB-pattern based cases                  │
│  • Pixel difference threshold               │
│  • Reduces statistical anomalies            │
└─────────────────────────────────────────────┘
                    ▼
Layer 3: Edge-Adaptive Enhancement
┌─────────────────────────────────────────────┐
│  Edge-Prioritized Embedding                 │
│  • Sobel edge detection                     │
│  • High-gradient region priority            │
│  • Perceptual masking                       │
│  • Histogram preservation                   │
└─────────────────────────────────────────────┘
                    ▼
            Secure Stego Image
```

---

## 📈 Evaluation Pipeline

```
┌────────────┐         ┌────────────┐
│   Cover    │         │   Stego    │
│   Image    │         │   Image    │
└─────┬──────┘         └─────┬──────┘
      │                      │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │   metricscalc.py     │
      │                      │
      │  Quality Metrics     │
      ├──────────────────────┤
      │  • PSNR              │
      │  • MSE               │
      │  • Entropy           │
      │  • Histogram Dev     │
      │  • Capacity          │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │  Quality Assessment  │
      │                      │
      │  Excellent / Good /  │
      │  Fair / Poor         │
      └──────────────────────┘

      ┌──────────────────────┐
      │  steganalysis.py     │
      │                      │
      │  Attack Simulations  │
      ├──────────────────────┤
      │  • RS Analysis       │
      │  • Histogram         │
      │  • Chi-Square        │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ Robustness Score     │
      │                      │
      │ Detected / Not       │
      │ Detected             │
      └──────────────────────┘
```

---

## 🗂️ File Dependency Graph

```
main.py
├── AESCTR.py
│   └── pycryptodome (Crypto.Cipher.AES)
├── adaptive_stego.py
│   ├── numpy
│   └── opencv-python (cv2)
├── metricscalc.py
│   ├── numpy
│   └── opencv-python (cv2)
└── steno.py (optional)
    ├── PIL (Pillow)
    └── cryptography

run_steganalysis.py
└── steganalysis.py
    ├── numpy
    ├── opencv-python (cv2)
    └── matplotlib

compare_methods.py
├── AESCTR.py
├── adaptive_stego.py
├── steno.py
├── metricscalc.py
└── steganalysis.py
```

---

## 🎨 Conceptual Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   ENHANCED STEGANOGRAPHY SYSTEM             │
│                                                             │
│  ┌────────────┐   ┌────────────┐   ┌──────────────────┐   │
│  │   Cover    │   │  Secret    │   │    AES Key       │   │
│  │   Image    │   │  Message   │   │  (save securely) │   │
│  └─────┬──────┘   └─────┬──────┘   └─────────┬────────┘   │
│        │                │                     │            │
│        │                ▼                     │            │
│        │         ┌──────────────┐             │            │
│        │         │  AES-CTR     │             │            │
│        │         │  Encryption  │◄────────────┘            │
│        │         └──────┬───────┘                          │
│        │                │                                  │
│        │                ▼                                  │
│        │         ┌──────────────┐                          │
│        │         │  Encrypted   │                          │
│        │         │  Payload     │                          │
│        │         └──────┬───────┘                          │
│        │                │                                  │
│        ▼                ▼                                  │
│  ┌─────────────────────────────────┐                      │
│  │  Adaptive LSB-MSB Steganography │                      │
│  │  + Edge Enhancement             │                      │
│  └────────────┬────────────────────┘                      │
│               │                                            │
│               ▼                                            │
│        ┌──────────────┐                                    │
│        │  Stego Image │                                    │
│        └──────────────┘                                    │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │              EVALUATION LAYER                    │     │
│  │                                                  │     │
│  │  Quality Metrics  │  Steganalysis Tests        │     │
│  │  • PSNR           │  • RS Analysis             │     │
│  │  • MSE            │  • Histogram               │     │
│  │  • Entropy        │  • Chi-Square              │     │
│  │  • Capacity       │                            │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 🔧 Component Hierarchy

```
IS_Proj/
│
├─── USER INTERFACE
│    ├─── main.py (primary entry point)
│    ├─── run_steganalysis.py (evaluation tool)
│    └─── compare_methods.py (comparison tool)
│
├─── CORE ALGORITHMS
│    ├─── adaptive_stego.py ⭐ (main algorithm)
│    │    ├─── AdaptiveSteganography class
│    │    │    ├─── Edge detection module
│    │    │    ├─── Block decomposition
│    │    │    ├─── Mean-of-medians computation
│    │    │    ├─── MSB case detection
│    │    │    └─── Adaptive embedding/extraction
│    │    │
│    ├─── AESCTR.py (encryption)
│    │    ├─── aes_ctr_encrypt()
│    │    └─── aes_ctr_decrypt()
│    │
│    └─── steno.py (legacy basic LSB)
│
├─── EVALUATION
│    ├─── metricscalc.py (quality metrics)
│    │    ├─── PSNR / MSE
│    │    ├─── Entropy
│    │    ├─── Capacity
│    │    └─── Histogram deviation
│    │
│    └─── steganalysis.py (attacks)
│         ├─── RSAnalysis class
│         ├─── HistogramAnalysis class
│         └─── ChiSquareAttack class
│
├─── DOCUMENTATION
│    ├─── README.md (main guide)
│    ├─── QUICK_START_GUIDE.md
│    ├─── IMPLEMENTATION_ANALYSIS.md
│    ├─── SUMMARY.md
│    ├─── INDEX.md
│    └─── ARCHITECTURE.md (this file)
│
└─── CONFIGURATION
     └─── requirements.txt
```

---

## 🚀 Execution Flow

### Main Program Flow

```
python main.py
│
├─► Load cover image
├─► Get user input (message, edge_threshold)
├─► Encrypt message (AES-CTR)
├─► Embed using adaptive method
├─► Save stego image
├─► Calculate quality metrics
├─► Display results
├─► Extract and verify
└─► Optional: Compare with basic LSB
```

### Steganalysis Flow

```
python run_steganalysis.py cover.jpg stego.png
│
├─► Load images
├─► Run RS Analysis
│   └─► Calculate RM, SM, RN, SN
├─► Run Histogram Analysis
│   └─► Calculate chi-square, KS statistic
├─► Run Chi-Square Attack
│   └─► Test Pairs-of-Values
├─► Generate summary
└─► Optional: Create visualization
```

### Comparison Flow

```
python compare_methods.py
│
├─► Get message input
├─► Encrypt message once
├─► Test Method 1: Basic LSB
├─► Test Method 2: Adaptive LSB-MSB
├─► Test Method 3: Edge-Enhanced
├─► Calculate metrics for all
├─► Run steganalysis on all
├─► Generate comparison table
└─► Display recommendations
```

---

**Document**: System Architecture  
**Purpose**: Visual understanding of system structure  
**Last Updated**: December 19, 2025

[Back to INDEX](INDEX.md) | [README](README.md)
